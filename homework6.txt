作业题目6：重写溢出中断服务程序
背景：
1、了解标志寄存器；
2、理解一次计算，硬件会同时根据运算结果置进位/借位标志CF（无符号数）和溢出
    标志(有符号数)，当OF被置1表示运算结果错误；
3、C语言对溢出是NB行为，程序会有一定风险；

基本要求：
1、C语言中用内联汇编解决溢出报错；
2、不用JO或JNO逻辑(上节课课堂作业:实现有符号数加法运算，发生溢出时（OF=1）能报错（数据宽度自定义，字节，字都可以）)
3、要求重写INTO（4号中断）的中断服务程序；
目前INTO中断服务程序没实际内容，如下图所示：
0070:0008 FE38      ??? [BX+SI]
0070:000A 2C00      SUB AL,00
0070:000C CF        IRET